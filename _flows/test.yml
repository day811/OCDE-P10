id: test
namespace: company.team
description: "Test Flow"

# ===== CONFIGURATION GLOBALE D'ERREURS (Namespace level) =====

tasks:
  - id: send_mail
    type: io.kestra.plugin.email.MailSend
    to: day811@laposte.net
    subject: Test
    host: "{{ secret('SMTP_HOST') }}"
    transportStrategy: SMTP_TLS
    password: "{{ secret('SMTP_PASSWORD') }}"
    username: "{{ secret('SMTP_USER') }}"
    port: "587"
    htmlTextContent: |-
      <body><title>Pipeline ocde_p10 a échoué!</title>
      <p>Execution: </p>
      <p>Voir: </p>
      </body>
    from: day811@laposte.net
    plainTextContent: |-
      Pipeline ocde_p10 a échoué!
      Execution: 
      Voir: https://kestra-ui/executions/
